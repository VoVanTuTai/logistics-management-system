export const queryKeys = {
  dashboard: ['dashboard'],
  shipments: ['shipments'],
  pickups: ['pickups'],
  tasks: ['tasks'],
  manifests: ['manifests'],
  ndr: ['ndr'],
  tracking: ['tracking'],
} as const;

